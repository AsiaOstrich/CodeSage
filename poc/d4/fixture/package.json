{
  "name": "codesage-d4-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "XSPEC-237 D4 PoC fixture — a small multi-file TS library with a non-trivial cross-file call graph, used as the brownfield base for measuring CodeSage call-chain injection into the Builder.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
